FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir scrapling fastapi uvicorn[standard] httpx

COPY docker/scrapling_service.py /app/scrapling_service.py

EXPOSE 8001

CMD ["uvicorn", "scrapling_service:app", "--host", "0.0.0.0", "--port", "8001"]
